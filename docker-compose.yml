services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - APP_ENV=development
      - RESTORATION_TTL_SECONDS=3600
    volumes:
      - ./configs:/app/configs
    restart: unless-stopped